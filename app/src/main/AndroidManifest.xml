<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kleog.fitnessapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light">
        <activity
            android:name=".Views.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Views.GraphPage" />
        <activity android:name=".Views.MealActivity" />
        <activity android:name=".Views.SearchActivity" />
        <activity android:name=".Views.QuantityActivity" />
        <activity android:name=".Views.ExerciseActivity" />
        <activity
            android:name=".Views.newGraphActivity"
            android:label="@string/title_activity_new_graph"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>